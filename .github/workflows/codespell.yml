name: Codespell
on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
      - stable-*.*
      - dev-*.*
jobs:
  codespell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: codespell-project/actions-codespell@v2
